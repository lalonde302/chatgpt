# Chatbot Interface

This repository contains a simple HTML page that demonstrates a minimal chatbot interface.

Open `index.html` in a browser to try it out. The page allows you to type a message and see a placeholder response from the bot.
